//Este comentario dice que hace la funcion ¿Les parece que sirve para algo en produccion?
function HacerSuma(primerNumero, segundoNumero) {
    //Este tampoco sirve para nada
    return parseInt( primerNumero) + parseInt(segundoNumero);
}